class book{
public:
	enum isOpen{open, closed};
int numPages(); const{return numPages;}
string title(); const{return title;}

isOpen(isOpen isopen){
if (isOpen == open)
surfaceArea = 10;
if (isOpen == closed)
surfaceArea = 5;
}

private:
isOpen isopen;
int numPages, surfaceArea;
string title;
	};
	
